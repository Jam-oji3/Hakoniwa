# 立体アイロンビーズ特化CAD 「ハコニワ」設計書

> **ソース:** [Notion 設計書](https://app.notion.com/p/3cabae9a2c64801dadc7ca40cb4cbd66)
>
> **取得日:** 2026-09-10

---

# 要件定義

> 🎯 本セクションは「欲しい仕様」を実装・設計に渡せる形へ整理したものです。上記のコンセプトは原文のまま残し、本要件はその意図を具体化します。

## 1. 目的と初期リリースの範囲

- 四角プレートとアイロンビーズによる立体作品を、**設計から実製作図まで一貫して扱えるCAD**として提供する。
- 初期対象プラットフォームは **Windows 11 向け .exe** とする。
- 初期スコープは、四角プレートを用いるボクセルアート的な製作方法に限定する。片面のみをアイロンして円筒状に巻く等の特殊技法は対象外とする。
- 29×29マスの大判四角プレートを基準単位とする。ただし、複数プレートを連結して製作できるため、**29×29は作品・ピースのサイズ上限ではない**。

## 2. 基本概念とデータモデル

| 概念 | 定義・役割 | 製作可能性 |
| --- | --- | --- |
| **Project** | 作品全体。構成要素、配色、製作図、設定を保持する。 | 単体では判定しない |
| **Assembly** | 作品の組立状態。ShapeとPieceを空間内に配置し、完成形と組立関係を管理する。 | Pieceのみで構成されるとき出力可能 |
| **Shape** | 自由な3Dボクセル造形。完成形を素早く検討するための設計用オブジェクト。 | 未確定。PDF出力前にPieceへ分割する必要がある |
| **Piece** | 単一のローカル平面上に全ビーズが存在する、実際に一枚のプレート上で作れる最小の製作単位。 | 確定。2D図案・必要ビーズ数・組立手順の出力対象 |
| **Bead** | 色を持つ最小セル。ShapeまたはPieceに所属する。 | Pieceに所属する場合に製作図へ反映 |

- ShapeとPieceは、移動・回転・色・表示／非表示・選択・階層・Undo／Redo等を共有するボクセルオブジェクトとして扱う。
- Pieceには「全ビーズが単一平面上にある」という不変条件を適用する。
- PDFなどの製作成果物を出力する時点で、未分割のShapeが存在する場合は出力を止め、対象Shapeと分割が必要な理由を示す。

## 3. 作成ワークフロー

### 3.1 Piece-first：製作単位から組み立てる

1. 新しいPieceを作成する。
2. Piece専用の2Dエディタで図案・色を編集する。
3. 3D AssemblyビューでPieceを配置・回転し、他のPieceと組み立てる。
4. 完成形をプレビューし、構造チェックと製作図出力を行う。

- 裏側など3Dビューでは選択しにくいビーズも、パーツツリーからPieceを選び2D編集画面を開くことで直接編集できる。
- 製作方法が最初から分かっている作品に適する。

### 3.2 Shape-first：自由造形してから製作単位へ分割する

1. Shapeとして3D空間に自由なボクセル形状を作成・編集する。
2. 分割支援を使い、Shapeを複数のPieceへ変換する。
3. 各Pieceを2Dエディタで調整し、Assembly上で接合・組立を確認する。
4. Shapeが残っていないことを確認して製作図を出力する。

- 分割方法は、XY／XZ／YZ平面による分割、選択面のPiece化、手動選択によるPiece化を初期候補とする。
- 完成形の検討を優先したい作品、箱形状や複雑なシルエットの高速モデリングに適する。
- どちらの経路でも、最終的な成果物はAssembly配下のPiece群として同じデータ構造に収束する。

## 4. 機能要件

### 4.1 モデリング・編集

- 3Dボクセルの配置、削除、色変更、選択、移動、回転を行えること。
- Pieceの2Dエディタで、平面図案を作成・編集できること。
- ShapeからPieceへの分割、およびPieceの配置・組立ができること。
- 3Dビューではボクセル状態とビーズ外観を切り替えて表示できること。可能であれば、アイロン後に潰れた見た目もプレビューに含める。
- カメラ操作、拡大縮小、表示／非表示、選択中オブジェクトへのフォーカスを提供すること。

### 4.2 階層・表示管理

- Project内のShape、Piece、グループをツリー形式で管理できること。
- グループ／サブグループを任意に作成し、パーツを整理できること。
- 任意の階層・サブパーツだけを分離表示できること。Blenderのローカルビューに近い挙動を目標とする。
- ツリーからPieceを選んで2Dエディタを開けること。
- PDF出力は、ツリー上の小さな製作単位（原則として末端Piece）ごとに行えること。

### 4.3 カラーと必要ビーズ数

- Perler、Hama、fuse、nano等のメーカー別カラーパレットを選択・適用できること。色名・色番号・利用条件等の権利・ライセンスは別途確認する。
- 同等色の扱い、カスタムパレット、パレット未登録色の表示方針は詳細設計で定義する。
- Piece単位・グループ単位・Project全体で、色別および合計の必要ビーズ数を集計できること。
- 集計結果を画面表示および製作図へ反映できること。

### 4.4 PDF製作図・組立ガイド

- Pieceごとに2D図案PDFを出力できること。
- PDFには少なくとも、Piece名／識別子、マス目、各マスの色識別、サイズ、色別必要数を含めること。
- 大きなPieceは29×29を超えても扱えること。プレート連結が必要な場合は、連結単位・ページ分割・位置合わせを製作図上で分かるようにする。
- Assemblyの構造に基づき、Pieceを作る順序と接合・組立の順序を示す、LEGO説明書に近い組立ガイドを出力できることを目標とする。
- Shapeが残存する場合は、どのShapeをPiece化すべきかを示して出力を拒否すること。

### 4.5 構造チェック

- 解析は「壊れないことの保証」ではなく、製作時に注意すべき弱点を早期に知らせる支援機能として提供する。
- 少なくとも次の候補を検出・指摘できること。
  - 接合部の嵌合深さが浅い、または接合面積が小さく、ぐらつきや横倒れが起きやすい箇所
  - 1マス単位の凹凸だけに依存する大きな接合。必要に応じて2マス以上の嵌合、ダボ、裏面支持などを提案する
  - 支持されていない張り出し、重心に対して支えが不足する構造
  - 大きな平面Pieceの中央部など、たわみ・歪みが生じやすい領域
  - 接合が十分に挟み込まれていない、または固定方向が不足する箇所
- 指摘には、該当箇所を3Dビューで強調表示し、理由と改善方向を併記する。
- 閾値や判定ロジックは、検証・試作を踏まえて調整可能にする。

## 5. 非機能要件・前提

- Windows 11環境で一般的な作品サイズを実用的な操作感で編集・プレビューできること。
- 保存、読み込み、Undo／RedoでShape、Piece、階層、色、配置、接合関係を一貫して復元できること。
- 対象はWindows 11向け .exeに限定する。モバイル展開は現時点のロードマップに含めない。
- パレットの正確性、実際のビーズ・プレート寸法、アイロン後の変形、構造チェックの精度は、実物試作で継続的に検証する。

## 6. 初期リリースの受入条件

- ユーザーがPiece-firstとShape-firstのいずれからも、複数Pieceで構成された作品を作成できる。
- 各Pieceが単一平面制約を満たし、残存ShapeがあればPDF出力前に明示される。
- ツリーでパーツを階層管理し、任意のPieceを2Dで編集できる。
- 3Dプレビュー、色別ビーズ集計、Piece別PDF製作図を利用できる。
- 29×29を超えるPieceでも、連結前提の製作情報を確認できる。
- 代表的な接合浅さ・支持不足・大平面のたわみリスクを検出し、該当箇所と改善方向を確認できる。

---

## 7. 決定事項（初期リリース）

### 7.1 空間・接合モデル

- 座標系はボクセル格子とし、Pieceの姿勢は **XY／XZ／YZの直交3平面に限定**する。任意角度のPieceは初期対象外とする。
- 接合情報をユーザーが個別に入力することを基本にはしない。Piece同士のボクセル形状・相対配置から接触部を検出し、直交する平面の接触は凹凸・差し込みによる接合候補として推定する。
- 推定された接合部について、接触面積、嵌合深さ、支持方向を構造チェックに使用する。必要に応じてユーザーが接合候補を確認・無視できる余地は詳細設計で設ける。

### 7.2 Shape分割

- ShapeからPieceへの**自動分割・最適分割は将来機能**とする。これは独立して大きな価値を持つ主要機能であり、初期リリースの必須範囲には含めない。
- 初期リリースでは、手動選択、選択面のPiece化、XY／XZ／YZ平面による分割支援を提供する。

### 7.3 ビーズ規格の扱い

- 初期版の基本モデルはメーカーに依存しないボクセルとし、パレット切替は主に色の選択・集計に用いる。
- 実寸のビーズ径・ピッチは、初期版の図案編集・基本プレビューには必須としない。
- ただし構造解析の精度向上に有利と判断された場合は、規格ごとのビーズ径等を任意の解析パラメータとして導入できる設計にする。

### 7.4 PDF出力

- PDFは末端Pieceだけでなく、選択したパーツ／サブパーツ単位で同一PDFにまとめて出力できることを初期リリース要件とする。
- PDFにはPiece別の図案・色別集計に加え、Assemblyから導出した組立順・接合箇所を含める。
- 組立図の出力はチェックボックスで有効／無効を切り替えられること。出力時はLEGO説明書のように段階的な組立図を目標とする。

### 7.5 保存形式・将来の入出力

- プロジェクトは専用拡張子の**単一ファイル**として保存・読み込みできることを初期リリース要件とする。
- このファイルには、Shape／Piece、階層、色、配置、接合推定に必要な情報、出力設定を含める。
- 他形式からのインポートは将来機能とし、初期リリースの対象外とする。

### 7.6 代表受入シナリオ：ハンマー形状

- 初期版の代表作品は、大きめの直方体のハンマー本体に、平面の柄が刺さったハンマー状の形とする。
- 柄も直方体として作成し、ハンマー本体と柄は別Pieceとしてパーツ分けする。
- この作品により、Piece-firstまたはShape-firstの作成、直交面接合の推定、Piece別・サブパーツ別PDF、組立図、そして柄の接合が浅い場合の警告を一連で確認する。

---

## 9. 技術方針・アーキテクチャ（決定）

### 9.1 採用技術

- **言語：Rust**。TexFlowと同じ基盤を採用する。
- **UI：egui／eframe**。CADに必要なドッキングパネル、ツリー、ツールバー、インスペクタ、2Dエディタを構成する。
- **3D描画：wgpu**。WindowsではDirectX 12を使う。初期対象はWindows 11だが、将来Macが必要になればMetalバックエンドを利用できる。
- **PDF：printpdf等によるベクターPDF生成**。
- **保存：**`.ibcad`**形式のZIPコンテナ**。内部に`project.json`、パレットのスナップショット、サムネイル等を持つ。
- **配布：Windows x64向けの自己完結型単一.exe**。

### 9.2 レイヤード・アーキテクチャ

- **Domain**：Project／Assembly／Shape／Piece／Bead、階層、接合推定、構造チェック、集計、組立順。egui・wgpu・ファイルI/Oに依存させない。
- **Application**：「ビーズを置く」「Piece化する」「PDFを書き出す」などをCommandとして実行し、Domainの状態を変更する。
- **Infrastructure**：`.ibcad`の読み書き、PDF出力、パレット、設定を担う。
- **Presentation**：egui画面、wgpuレンダラー、カメラ、選択、ショートカットを担う。Domainを直接変更せず、ApplicationのCommandを呼ぶ。

### 9.3 適用パターン

- **Entity／Value Object**：PieceやShapeは安定したIDを持つEntity。座標・色・姿勢・サイズは小さく不変なValue Object。
- **Command + Undo／Redo**：追加、削除、塗り替え、移動、Piece化、階層変更を操作単位として履歴化する。
- **Reducer風の状態更新**：Commandを受け、Project状態を決定的に更新する。UIイベントからデータを直接変更しない。
- **Repository**：保存・読込を抽象化し、最初はIbcadRepositoryを実装する。将来のimportに備える。
- **Strategy**：接合推定、構造チェック、PDF組立順、パレット変換を差し替え可能な規則にする。
- **Adapter**：DomainのPiece／Beadをwgpu用メッシュ・インスタンス・選択IDに変換し、描画実装を隔離する。
- **Dirty region**：変更されたPieceと近傍だけを再解析・再メッシュ化する。

### 9.4 状態と描画の原則

- Project状態を唯一の正とし、描画メッシュ・選択ハイライト・構造警告はそこから導出するキャッシュとして扱う。
- ビーズを一個ずつ描画オブジェクトにせず、Piece／Shape単位のメッシュまたはGPUインスタンシングで描画する。
- 選択・警告・分離表示はレンダラー固有の一時状態とし、作品ファイルには必要な論理状態だけを保存する。
